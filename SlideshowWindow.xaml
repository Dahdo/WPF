<Window x:Class="WPFLab2.SlideshowWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFLab2"
        mc:Ignorable="d"
        Title="SlideshowWindow" Height="768" Width="1024" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" WindowStyle="None" ResizeMode="NoResize">
    <Grid x:Name="imageGrid">
        <Image x:Name="imageCanva" Stretch="UniformToFill" />
        <Grid.ContextMenu>
            <ContextMenu x:Name="gridContextMenu">
                <MenuItem Header="Play/Pause Slideshow" Click="PlayPauseSlideshow_Click" />
                <MenuItem Header="Stop Slideshow" Click="StopSlideshow_Click" />
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</Window>
